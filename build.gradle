plugins {
    id 'java'
    //id 'com.github.johnrengelman.shadow' version '8.1.1'
}

group = 'com.puvn'
version = '1.0-SNAPSHOT'

repositories {
    mavenCentral()
}

dependencies {
    implementation group: 'com.google.guava', name: 'guava', version: '33.3.0-jre'

    testImplementation platform('org.junit:junit-bom:5.10.0')
    testImplementation 'org.junit.jupiter:junit-jupiter'
}

test {
    useJUnitPlatform()
}

/*
// Task to create a fat JAR (uber JAR)
shadowJar {
    archiveClassifier.set('all') // Adds a classifier to differentiate the fat JAR
    mergeServiceFiles() // Ensures that services such as logging work correctly
}

// Set the default task to build the shadow JAR
jar {
    enabled = false
}
build.dependsOn shadowJar*/
